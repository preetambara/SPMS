export const ROLES = Object.freeze({
  STUDENT: "student",
  FACULTY: "faculty",
  ADMIN: "admin",
});

export const ROLE_LABELS = Object.freeze({
  [ROLES.STUDENT]: "Student Portal",
  [ROLES.FACULTY]: "Faculty Portal",
  [ROLES.ADMIN]: "Admin Console",
});
